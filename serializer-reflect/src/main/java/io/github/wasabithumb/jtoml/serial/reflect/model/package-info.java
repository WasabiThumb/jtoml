/**
 * Utilities used by the JToml reflect serializer
 * to inspect the "shape" of Java types (if/how they
 * expose inner elements/properties).
 */
@NullMarked
package io.github.wasabithumb.jtoml.serial.reflect.model;

import org.jspecify.annotations.NullMarked;